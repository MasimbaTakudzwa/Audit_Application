{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the auditor workspace window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
